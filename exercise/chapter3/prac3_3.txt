T3.3:
cin忽略空格，从读到第一个真正的字符开始，一直到下一个空格为止。
getline读取一整行，包括空格，直到遇到换行符为止。

T3.7:
char类型依然是副本，修改不了，要加上&才可以。

T3.9:
string s;
cout << s[0] << endl;
不合法，s为空字符串，用0去索引错误。

T3.10:
const string s = "Keep out!";
for (auto &c : s) {/* ... */}
合法，c是char&.